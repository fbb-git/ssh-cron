Ssh-cron is a cron daemon allowing connections to remote computers requiring
the use of ssh keys with passphrases.

Ssh-cron's repository has moved to https://gitlab.com/fbb-git/ssh-cron
and is not maintained at this location anymore.

The project's web-page can be found at 
http://fbb-git.gitlab.io/ssh-cron/
